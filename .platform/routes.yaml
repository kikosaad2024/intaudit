# routes.yaml
"https://{default}/":
    type: upstream
    upstream: "varnish:http"
    cache:
        enabled: true          # تمكين التخزين المؤقت
        default_ttl: 3600      # مدة التخزين المؤقت الافتراضية (ساعة واحدة)
